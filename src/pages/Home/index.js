import styled from "styled-components";

const HomeWrapeper = styled.div`
    width: 100%;
    height: 200vh;
    background-color: var(--main-background);
`

export default function Home () {
    return (
        <HomeWrapeper>
            home sec
        </HomeWrapeper>
    )
}